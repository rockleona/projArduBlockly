Blockly.JavaScript['pinsetting'] = function(block) {
    var dropdown_pinnumber = block.getFieldValue('pinNumber');
    var dropdown_pinset = block.getFieldValue('pinSet');
    // TODO: Assemble JavaScript into code variable.
    var code = '...;\n';
    return code;
  };

Blockly.JavaScript['pinstatus'] = function(block) {
    var dropdown_pinnumber = block.getFieldValue('pinNumber');
    var dropdown_digitalwrite = block.getFieldValue('digitalWrite');
    // TODO: Assemble JavaScript into code variable.
    var code = '...;\n';
    return code;
  };

Blockly.JavaScript['ledstatus'] = function(block) {
    var dropdown_pinnumber = block.getFieldValue('pinNumber');
    var dropdown_name = block.getFieldValue('NAME');
    // TODO: Assemble JavaScript into code variable.
    var code = '';
    return code;
  };